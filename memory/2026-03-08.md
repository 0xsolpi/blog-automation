# 2026-03-08 (Asia/Seoul)

## Pipeline / Trigger Policy Updates
- New 5th front-door trigger added: `ㅎㅇㅅ.<제품쿠팡링크>`.
- Route for `ㅎㅇㅅ`: Nere → Ace → Nami → Zoro → Nere approval gate → Admin approval → Robin publish.
- Publish category mapping expanded:
  - `ㄱㄱ/ㄴㄴ/ㄹㅋ` → category `1`
  - `ㅎㄷ` → category `3`
  - `ㅎㅇㅅ` → category `9`
- `ㅎㅇㅅ` runs use a **relaxed validation mode** in Ace:
  - Review count / rating / best-review count / best-review image count are not blocking hard-gates.
  - Collect best-review text/images as much as possible under existing logic; 부족분은 `validation checklist warn`으로 기록하고 진행 허용.
  - Hard-block still applies for direct link mismatch and partner deeplink generation failure.
- Zoro review policy updated for `ㅎㅇㅅ`:
  - `validation warn` on review/rating/best-review quantity is not a standalone FIX/FAIL reason.
  - Existing hard-gates (link/partner-link, style/structure/consistency, CTA/compliance) remain enforced.

## Title Quality Rules (Nami + Zoro)
- Added title constraints to prevent robotic patterns:
  - Ban `(모델: XXX)` parenthetical machine format.
  - If productName already includes model phrase, avoid duplicate model repetition.
  - Preserve productName key phrase continuity (no broken/omitted core phrase in title).
- Added SEO-oriented title guardrails:
  - Front part should include buyer-filter signals (target/situation/category).
  - Prefer concrete selection criteria over hype words.
  - One title should target one primary search intent.
  - Avoid unstable year/price/discount claims unless maintainable.
  - Enforce title uniqueness per URL.
  - Enforce consistency between title(H1) promises and first key comparison table criteria.
- Zoro now FIXes violations of the above title rules.

## Production Results
- `FD-20260307-232350-HD-8eac66` published at `https://0xsolpi.com/20260307-p361/` with category `3`.
- `FD-20260308-002928-HYS-1c94df` published at `https://0xsolpi.com/20260308-p371/` with category `9` (`published_with_fixes`).

## UX Experiment (CTA Visibility)
- On latest post (`p371`), Coupang partner links were tested as button-style CTAs (multiple visual variants) to improve link recognizability.
- Status: style experiments ongoing; no final global style lock yet.
