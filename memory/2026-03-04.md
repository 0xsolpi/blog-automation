# 2026-03-04

- 운영 정책이 `autopilot-v1`로 고정됨: 선장님 개입은 ① 루피 리서치 요청 ② 아이템 선택만 수행, 이후 파이프라인은 자동 진행.
- 자동 파이프라인 기본 흐름이 재정의됨: `루피 -> 에이스 -> 나미 <-> 조로(최대 3회 수정루프) -> 로빈 업로드 -> 네레 최종 결과 보고`.
- 로빈 역할이 다시 Primary Uploader로 복귀됨. (이전 IDLE/보조 상태 해제)
- No-Regression 업로드 안전 규칙 고정:
  - `STARTED -> IN_PROGRESS -> DONE` 이벤트 강제
  - 2분 무변화 시 자동 재시도
  - 5분 내 URL 미산출 시 실패 처리
  - 2회 실패 시 `BLOCKED` + 백업 경로 전환
  - 완료 보고 필수값: `post_url`, `published_at`, `render_check`, `tabs_cleanup: done`
- 품질 게이트 필수 기준 재확인:
  - placeholder 0건
  - `.md`/`../` 내부링크 0건
  - 이미지 렌더 OK
  - 제휴 고지문 원문 일치
- 에이스 브라우저 위생 규칙 추가: 작업 종료 시 탭 정리 필수(`tabs_cleanup: done` 보고).
- 문서/정책 반영 커밋들:
  - `58ba1a7`: no-regression upload safety watchdog 규칙 반영
  - `e34467f`: robin primary uploader 기준으로 autopilot 프로세스 재정립
