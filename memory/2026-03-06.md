# 2026-03-06 Memory (KST)

## 파이프라인/발행 안정화
- `scripts/wp_rest_publish.py`를 WordPress media 업로드 **multipart(files=...)** 방식으로 패치함.
- 업로드 전 파일 검증(존재/일반파일/0바이트) 추가.
- `scripts/publish_preflight.py`를 본문 + `image_manifest` 동시 검사로 확장함.
  - `image_manifest.images[].local_path` 존재/파일형태/0바이트 검증 포함.
- `scripts/publish_guard.sh`에 `MANIFESTS` 입력 연동.
- 운영 문서 `ops/robin_publish_contract_v3.md`에 preflight(본문+manifest) 기준 반영.
- `2026-03-05-1210-bomnaduli-luffy-001` 런은 최종적으로 publish 성공 상태까지 갱신됨.

## Self-Improvement 운영
- `self-improving-agent` 스킬 반영.
- `.learnings/LEARNINGS.md`, `.learnings/ERRORS.md`, `.learnings/FEATURE_REQUESTS.md` 생성.
- `AGENTS.md` 및 `agents/*.md`에 공통 self-improvement 규칙과 업로드 안정화 규칙 반영.

## Discord 멀티봇(2안) 운영 결정
- 에이전트별 디스코드 봇 계정 분리 운영으로 확정.
- 공용 채널 `#작업논의` ID: `1479149504241340426`.
- 정책 확정:
  - 각 에이전트 개인 채널: 멘션 없이 반응
  - `#작업논의`: **멘션 있을 때만** 반응
- `openclaw.json`에 discord accounts/bindings/guild channel allowlist 반영 완료.

## 보안/운영 주의
- `.env.discord.template`에 실제 토큰이 기입된 상태로 사용됨.
- 토큰/앱 비밀번호는 설정 안정화 후 **로테이션(재발급)** 권장.

## 운영 정책 추가 확정 (야간)
- 호칭 정책: 전 에이전트 호칭을 `사장님`에서 `선장님`으로 통일.
- 파이프라인 보고/명령 채널을 Discord `#1479149504241340426` 단일 채널로 고정.
- 핸드오프 시 다음 단계 에이전트 **멘션 지명 필수** 규칙 확정.
- Telegram 운영 중단: 채널 비활성/바인딩 제거 기준으로 Discord 단일 운영 유지.

## Luffy→Ace 구간 안정화 패치
- handoff 선택 키를 `selected_item_numbers` 단일 기준으로 고정.
- 금지/호환 키(`owner_selection`, `selection.selected_numbers`) 정책 명시:
  - Luffy는 생성 금지
  - Ace는 수신 시 정규화 후 처리 + 로그
- Ace 누락 감시 규칙 추가:
  - raw 생성 후 2분 내 processed 미생성 시 `IN_PROGRESS(manual_fallback)`
  - 10분 초과 시 `BLOCKED(processing_timeout)`

## 연결 상태 진단 메모
- `channels status`의 `connected=false`가 간헐적으로 관측되나, 실제 `message send` 테스트로 계정별 전송 성공을 확인(실사용 기준 정상).
- Discord `default` 계정 미설정 경고를 줄이기 위해 `default.enabled=false` 처리.
