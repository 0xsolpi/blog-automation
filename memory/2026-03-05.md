# 2026-03-05 (KST) Durable Memory

## 운영/정책 확정
- 사용자 가시 메시지 경로는 **Telegram 단일**로 고정.
  - webchat/openclaw-control-ui로 사용자 보고/승인요청/결과보고 금지.
  - 내부 ACK/announce는 사용자 채널 발신 금지.
  - 내부 이벤트가 webchat에서 생겨도 Telegram로 즉시 미러링 보고.
- Zoro 검수 결과(PASS/FIX/HOLD) 수신 즉시 선장님에게 Telegram 보고 고정:
  - `run_id`, `조로 판정`, `네레 판정(✅/⚠️/❌)`, `업로드 진행 필요 여부`.
- 승인 게이트 규칙 재확인:
  - Admin 승인 없이 Robin 업로드 금지.

## 파이프라인/역할 규칙
- 역할 책임 정리:
  - Nami: `wp-ready.md` + `image_manifest.json` + 이미지 파일 준비/핸드오프.
  - Zoro: 정합 검수.
  - Nere: Robin 전달 전 입력 패키지 존재 게이트 확인.
  - Robin: 업로드/치환/발행.
- Placeholder 정책 정렬:
  - 중간 단계(Nami/Zoro) placeholder 허용 맥락 존재.
  - 최종 성공 판정은 Robin 치환/검증 완료 보고(`run_id/post_url/status/published_at/failures`) 기준.

## 장애 및 재발 방지
- 장애 유형: Nami 산출물이 agent3 경로엔 있었으나 blog-automation handoff 경로에 미동기화되어 Robin이 입력 패키지 없음으로 BLOCKED.
- 재발 방지 적용:
  - `scripts/ensure_robin_input_package.py` 추가: run_id 기준 Nami 산출물을 Robin 경로로 동기화.
  - `ops/autopilot_v1_policy.md` 업로드 전 필수 검사 강화:
    - `nami_to_zoro.json`, `zoro_to_nere.json`, `admin_publish_approval.json`
    - `wp-ready.md`, `image_manifest.json`, `images/*` 실파일 존재
  - `scripts/publish_preflight.py` 강화:
    - `your-domain.com` 링크 차단
    - 상대경로 이미지(`./images/...`) 차단
    - raw markdown 노출 패턴 차단(`raw_markdown_exposed`)

## 운영 이슈 메모
- 2026-03-05 오전, 일부 포스트에서 markdown raw 노출/이미지 렌더 실패 이슈 재발.
- 원인 핵심은 업로드 직전 입력 패키지 누락 게이트 미검증(네레 게이트 책임)으로 확인.
- 복구/재업로드는 Robin hotfix로 진행했고, 이후 게이트 규칙 강화 완료.
