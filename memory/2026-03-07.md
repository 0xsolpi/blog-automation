# 2026-03-07

- Zoro 최종 검수 규칙을 고정함.
  - 입력 우선순위: `final.md` → `validation.json` → `state.json`
  - 품질 게이트 확대: AI티/번역투, 오타·비문, 의미불명 한자어/외국어(외계어성) 검수 포함
  - 파트너스 링크 규칙 검수 추가: 본문 1회 + CTA 1회(총 2회), CTA 문구 중복 금지
  - FIX/HOLD 시 `nami_fix_instructions[]` 필수
  - 재수정 루프 최대 3회, 초과 시 최종 FAIL 및 `#1479149504241340426` 보고

- Robin 발행 규칙을 v2로 고정함.
  - 로컬 이미지 수집 → WP Media API 업로드 → `source_url` 기반 치환
  - 성공 판정 강화: `post_url 200` + 본문 이미지 샘플 6개 이상 200 + 렌더 엑박 없음
  - 중요 이슈(OG/메타/인덱싱 일시 실패)는 `published_with_fixes`로 조건부 발행 허용
  - 보정 스케줄: 즉시(0~5분) / 10분 / 30분
  - 3차 실패해도 게시 유지, 미해결 항목만 보고 채널에 기록

- Nere 운영 문서 신규 추가.
  - `ops/nere_orchestrator_checklist_v2.md` 생성(단계별 게이트/명령 해석/장애 처리)
  - `ops/nere_admin_approval_report_template.md` 생성(30초 승인 의사결정 템플릿)

- Discord 운영 고정 사항 재확인.
  - 보고 채널 `#1479149504241340426` 단일화
  - 선장님 호칭 고정
  - Luffy/Ace/Nami/Zoro/Robin 최신 운영기준 ACK 수신 완료

- E2E 리허설(`2026-03-07-0046-e2e-rehearsal-001`) 결과 기록.
  - Ace validation에서 리뷰 데이터/API 제약으로 fail 성격 확인
  - Zoro 판정 HOLD, state `nami_fix/waiting_nami` 전환
  - 선장님 지시로 테스트는 해당 지점에서 종료, 실전 진행 중 보완하기로 결정

- Front-door 보고 포맷 고정(선장님 지시).
  - `ㄱㄱ.<키워드>` 결과는 앞으로 항상 루피 보고 양식 그대로 전달:
    - "전달 완료했습니다, 선장님 ✅"
    - `run_id`
    - `포맷: ADMIN_FIXED_12`
    - `Top3`
    - `Ace 상태: ace_validation / pending / ace`

- 운영 노이즈/상태 혼선 보완 규칙 확정.
  - 동일 run에서 `done/completed` 확인 시, 과거 `failed_blocking`/중간 IN_PROGRESS 로그는 참고용 처리
  - 사용자 채널은 `DONE/BLOCKED/승인요청/최종발행결과` 중심으로 축약 보고
  - `NO_REPLY`·중복 ACK·중복 완료문은 사용자 채널 전달 최소화
  - Ace 자동진입 게이트 강화:
    - `ace_validation/pending/ace` 60초 미실행 → 자동 재트리거
    - 재트리거 후 60초 미실행 → `BLOCKED reason=ace_autostart_missed`

- 글 렌더 안정성 규칙(표/고지문) 고정.
  - 표는 Markdown 파이프 표 노출 금지, HTML `<table>`만 허용
  - 구조 금지: `<p><table>...</table></p>`
  - 공정위 고지문은 인라인 스타일 유지 + `<div>` 단독 블록 배치
  - 구조 금지: `<p><div ...고지문...</div></p>`
  - Zoro 검수에 위 구조 위반 발견 시 FIX 강제

- 실전 run/발행 이력.
  - `2026-03-07-0913-camping-luffy-002` 최종 발행 성공
  - post URL: `https://0xsolpi.com/20260307-p295/`
  - 성공판정 3요건(포스트 200/이미지 샘플 6+/렌더 엑박 없음) 충족

- 선장님 문체 선호(지속 반영).
  - 본문이 과도하게 리포트/기계적 톤으로 통일되는 것 비선호
  - 후기 전달형 자연어 톤(예: "~하더라고요")을 부분적으로 섞되, 과도한 일괄 통일은 피함
